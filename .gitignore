# generated binaries
Distribution/oscam-*
Distribution/list_smargo-*

# backup/swap file for vi
*~
*.swp

# IDE resources
.vscode

# build directories
/build/

# mbedTLS is fetched as a tarball on demand — ignore everything in
# mbedtls/ except the version pin file.
/mbedtls/*
!/mbedtls/version.conf
/.mbedtls-cache/

# Currently enabled config items
webif/is_defined.txt

# Testing program
tests.bin
tests.bin.debug

# Patch files
*.patch
*.diff
